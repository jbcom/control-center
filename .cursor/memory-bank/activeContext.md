# Memory Bank Relocated

The authoritative memory bank now lives at `../memory-bank/activeContext.md` from the repository root. Please read and update that global file so all agents (background and interactive) stay in sync.

---
alwaysApply: true
description: Core control center rules and session initialization
globs: ["**/*"]
---

# jbcom Control Center - Core Rules

## 🚨 SESSION START

This is **jbcom-control-center**, the unified control center for:
- **jbcom packages**: Python/TypeScript/Go packages in `packages/`
- **FSC infrastructure**: Terraform/SAM in `ecosystems/flipside-crypto/`
- **Centralized configs**: Cursor rules synced to public repos

### First Steps

```bash
# 1. Check context
cat memory-bank/activeContext.md

# 2. Check progress
cat memory-bank/progress.md
```

## 🔑 Authentication

| Token | Usage |
|-------|-------|
| `GITHUB_JBCOM_TOKEN` | jbcom repos (default) |
| `GITHUB_FSC_TOKEN` | FlipsideCrypto repos |

```bash
# jbcom repos
GH_TOKEN="$GITHUB_JBCOM_TOKEN" gh pr list --repo jbcom/jbcom-control-center

# FlipsideCrypto repos (if needed)
GH_TOKEN="$GITHUB_FSC_TOKEN" gh pr list --repo FlipsideCrypto/<repo>
```

## 📁 Key Locations

| Path | Purpose |
|------|---------|
| `packages/` | All jbcom packages |
| `ecosystems/flipside-crypto/` | FSC infrastructure |
| `cursor-rules/` | Centralized rules (synced out) |
| `memory-bank/` | Session context |
| `.github/workflows/` | CI/CD |

## 🚫 Rules

### Don't
- ❌ Push to main without PR
- ❌ Manually edit versions (use conventional commits)
- ❌ Skip tests

### Do
- ✅ Use PRs for changes
- ✅ Update memory-bank on session end
- ✅ Use conventional commits with scopes

## 📚 Docs

- Packages: `docs/JBCOM-ECOSYSTEM-INTEGRATION.md`
- Releases: `docs/RELEASE-PROCESS.md`
- FSC infra: `docs/FSC-INFRASTRUCTURE.md`
- Tokens: `docs/TOKEN-MANAGEMENT.md`

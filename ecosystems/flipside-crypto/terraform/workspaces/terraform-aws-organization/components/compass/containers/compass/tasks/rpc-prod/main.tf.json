{
  "module": {
    "base": {
      "context": "${local.context}",
      "ecr_repository_urls": "${local.context.system_environment.ecr_repository_urls}",
      "image_tag": "${var.image_tag}",
      "records_dir": "records/${local.workspaces_dir}",
      "rel_to_root": "${local.rel_to_root}",
      "source": "git@github.com:FlipsideCrypto/gitops.git//terraform/modules/architecture/container/deployment/deployment-tasks",
      "tasks": "${local.context.pipelines.compass-rpc-prod.tasks}"
    }
  }
}
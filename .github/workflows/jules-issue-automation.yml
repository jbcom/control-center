name: Jules Issue Automation

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  issues: write

jobs:
  delegate-to-jules:
    if: github.event.issue.pull_request == null && contains(github.event.comment.body, '/jules')
    runs-on: ubuntu-latest
    steps:
      - name: Check for API Key
        id: check
        env:
          GOOGLE_JULES_API_KEY: ${{ secrets.GOOGLE_JULES_API_KEY }}
        run: |
          if [[ -z "$GOOGLE_JULES_API_KEY" ]]; then
            echo "available=false" >> $GITHUB_OUTPUT
          else
            echo "available=true" >> $GITHUB_OUTPUT
          fi

      - name: Acknowledge
        if: steps.check.outputs.available == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue comment ${{ github.event.issue.number }} --body "🤖 Received request to delegate to Jules. Creating session..."

      - name: Get Default Branch
        id: get_branch
        if: steps.check.outputs.available == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          DEFAULT_BRANCH=$(gh repo view $REPO --json defaultBranchRef --jq .defaultBranchRef.name)
          echo "branch=$DEFAULT_BRANCH" >> $GITHUB_OUTPUT

      - name: Create Jules Session
        if: steps.check.outputs.available == 'true'
        id: jules
        env:
          GOOGLE_JULES_API_KEY: ${{ secrets.GOOGLE_JULES_API_KEY }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          COMMENT_BODY: ${{ github.event.comment.body }}
        run: |
          # Extract the prompt after the /jules command
          PROMPT=$(echo "$COMMENT_BODY" | sed 's|/jules||' | xargs)

          TASK=$(cat <<EOM
          Address issue #${ISSUE_NUMBER}: $ISSUE_TITLE

          The user provided the following instructions:
          $PROMPT
          EOM
          )

          RESPONSE=$(curl -s -X POST "https://jules.googleapis.com/v1alpha/sessions" \
            -H "X-Goog-Api-Key: $GOOGLE_JULES_API_KEY" \
            -H "Content-Type: application/json" \
            -d "$(jq -n \
              --arg task "$TASK" \
              --arg repo "${{ github.repository }}" \
              --arg branch "${{ steps.get_branch.outputs.branch }}" \
              --arg title "Jules: Address issue #${ISSUE_NUMBER}" \
              '{
                prompt: $task,
                sourceContext: {
                  source: ("sources/github/" + ($repo | split("/")[0]) + "/" + ($repo | split("/")[1])),
                  githubRepoContext: { startingBranch: $branch }
                },
                automationMode: "AUTO_CREATE_PR",
                title: $title
              }')")

          SESSION_ID=$(echo "$RESPONSE" | jq -r '.id // empty')
          SESSION_URL="https://jules.google.com/session/$SESSION_ID"

          echo "session_url=$SESSION_URL" >> $GITHUB_OUTPUT
          echo "session_id=$SESSION_ID" >> $GITHUB_OUTPUT

      - name: Post Session Link
        if: steps.check.outputs.available == 'true' && steps.jules.outputs.session_id != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue comment ${{ github.event.issue.number }} --body "## 🤖 Jules Session Created

          ➡️ **[Monitor Session](${{ steps.jules.outputs.session_url }})**

          Jules will analyze the issue and create a new PR with the suggested fix."

      - name: Post API Key Missing
        if: steps.check.outputs.available == 'false'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue comment ${{ github.event.issue.number }} --body "⚠️ GOOGLE_JULES_API_KEY not configured. Cannot delegate to Jules."

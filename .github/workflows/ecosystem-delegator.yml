name: Ecosystem Delegator

# Issue delegation to AI agents
# /jules - Delegate to Google Jules
# /cursor - Delegate to Cursor Cloud Agent
# /sage - Ask the Sage

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  delegate-cursor:
    name: Delegate to Cursor
    if: |
      github.event.issue.pull_request == null && 
      contains(github.event.comment.body, '/cursor')
    runs-on: ubuntu-latest
    steps:
      - name: Check for API Key
        id: check
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
        run: |
          if [[ -z "$CURSOR_API_KEY" ]]; then
            echo "available=false" >> $GITHUB_OUTPUT
          else
            echo "available=true" >> $GITHUB_OUTPUT
          fi

      - name: Acknowledge
        if: steps.check.outputs.available == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          gh issue comment "$ISSUE_NUMBER" --body "⚡ Received '/cursor' command. Spawning Cursor agent..."

      - name: Parse Task
        id: task
        env:
          COMMENT_BODY: ${{ github.event.comment.body }}
        run: |
          TASK=$(echo "$COMMENT_BODY" | sed 's|/cursor[[:space:]]*||' | head -c 2000)
          echo "task=$TASK" >> $GITHUB_OUTPUT

      - name: Spawn Cursor Agent
        if: steps.check.outputs.available == 'true'
        id: cursor
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          TASK_DESC: ${{ steps.task.outputs.task }}
          REPOSITORY: ${{ github.repository }}
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          RESPONSE=$(curl -s -X POST "https://api.cursor.com/v0/agents" \
            -H "Authorization: Bearer $CURSOR_API_KEY" \
            -H "Content-Type: application/json" \
            -d "$(jq -n \
              --arg task "Fix issue #$ISSUE_NUMBER: $TASK_DESC" \
              --arg repo "https://github.com/$REPOSITORY" \
              --arg branch "$DEFAULT_BRANCH" \
              '{
                prompt: { text: $task },
                source: { repository: $repo, ref: $branch },
                target: { autoCreatePr: true, openAsCursorGithubApp: true }
              }')")
          
          AGENT_ID=$(echo "$RESPONSE" | jq -r '.id // empty')
          echo "agent_id=$AGENT_ID" >> $GITHUB_OUTPUT

      - name: Post Agent Link
        if: steps.check.outputs.available == 'true' && steps.cursor.outputs.agent_id != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          AGENT_ID: ${{ steps.cursor.outputs.agent_id }}
        run: |
          gh issue comment "$ISSUE_NUMBER" --body "## ⚡ Cursor Agent Spawned

          🆔 **Agent ID:** \`$AGENT_ID\`
          🔗 **Monitor:** [cursor.com/agents](https://cursor.com/agents)
          
          The agent will analyze the issue and create a PR."

      - name: Post API Key Missing
        if: steps.check.outputs.available == 'false'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          gh issue comment "$ISSUE_NUMBER" --body "⚠️ CURSOR_API_KEY not configured."

  invoke-sage:
    name: Invoke Sage
    if: |
      github.event.issue.pull_request == null && 
      contains(github.event.comment.body, '/sage')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Trigger Sage
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COMMENT_BODY: ${{ github.event.comment.body }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
        run: |
          QUERY=$(echo "$COMMENT_BODY" | sed 's|/sage[[:space:]]*||')
          gh workflow run ecosystem-sage.yml \
            -f query="$QUERY" \
            -f context_type="issue" \
            -f context_id="$ISSUE_NUMBER"
          
          gh issue comment "$ISSUE_NUMBER" --body "🧙 Sage workflow triggered. Response coming soon..."

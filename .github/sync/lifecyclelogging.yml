# Sync configuration for lifecyclelogging package
# Syncs from monorepo to public container repo

group:
  - files:
      - source: packages/lifecyclelogging/
        dest: .
        exclude: |
          .git
          .github
          __pycache__
          *.pyc
          .pytest_cache
          .tox
          .venv
          dist
          build
          *.egg-info
    repos: |
      jbcom/lifecyclelogging

name: Ecosystem Curator

# Nightly orchestration that leverages @agentic/control for:
# - Issue triage → spawn agents
# - PR processing → reviews, merges
# - Fleet management → monitor agents

on:
  schedule:
    - cron: '0 2 * * *' # Nightly at 2 AM UTC
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run mode'
        type: boolean
        default: false
      target_org:
        description: 'Target organization (empty = all)'
        type: string
        default: ''

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  orchestrate:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: '22'

      - name: Fleet Status
        uses: jbcom/nodejs-agentic-control/actions/agentic-orchestrator@main
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          command: summary

      - name: Run Curator Script
        env:
          GITHUB_TOKEN: ${{ github.token }}
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
          GOOGLE_JULES_API_KEY: ${{ secrets.GOOGLE_JULES_API_KEY }}
          OLLAMA_HOST: ${{ vars.OLLAMA_HOST || 'https://ollama.com' }}
          OLLAMA_API_KEY: ${{ secrets.OLLAMA_API_KEY }}
          DRY_RUN: ${{ inputs.dry_run }}
          TARGET_ORG: ${{ inputs.target_org }}
        run: |
          # Install @agentic/control for fleet management
          npm install -g @agentic/control@latest
          
          # Run curator with agentic-control
          node scripts/ecosystem-curator.mjs

      - name: Post Summary
        if: always()
        run: |
          if [ -f curator-report.json ]; then
            echo "### Ecosystem Curator Summary 🤖" >> $GITHUB_STEP_SUMMARY
            echo '```json' >> $GITHUB_STEP_SUMMARY
            cat curator-report.json >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi

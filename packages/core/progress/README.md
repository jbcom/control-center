# User Progress Schema

This directory defines the data structure for tracking a user's progress through the Professor Pixel curriculum.

## Files

- **`schema.json`**: A JSON Schema file that defines the valid structure for a user's progress file. This includes their total XP, current level, completed lessons, and unlocked achievements. All user progress data should be validated against this schema.

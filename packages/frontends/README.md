# Frontends

This package will house the various frontends for the Professor Pixel platform. The primary frontend will be the web-based version from `pixels-pygame-palace`, with WASM support for running pygame in the browser.
